apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-course-registration-deployment
  labels:
    app: student-course-registrartion
spec:
  replicas: 2   # Number of Pods (containers) to run
  selector:
    matchLabels:
      app: student-registration
  template:
    metadata:
      labels:
        app: student-course-registration
    spec:
      containers:
        - name: student-course-registration
          image: gaga70/student-registration:latest   # <-- Replace this
          ports:
            - containerPort: 5000
          env:
            - name: FLASK_ENV
              value: "production"